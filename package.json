{
  "name": "github-page",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "DEPLOY_ENV=GH_PAGES nuxt build",
    "generate": "DEPLOY_ENV=GH_PAGES nuxt generate",
    "start": "nuxt start"
  },
  "dependencies": {
    "core-js": "^3.9.1",
    "nuxt": "^2.15.3"
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
